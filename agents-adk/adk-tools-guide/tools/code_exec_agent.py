"""
Built-in Code Execution sketch:
- Enable a built-in code executor on the agent.
- Stream responses; look for executable_code and execution_result parts.
"""
# Pseudocode:
# from google.adk.code_executors import BuiltInCodeExecutor
# agent = LlmAgent(name="calculator", model="gemini-2.0-flash", instruction="Write minimal Python to compute.", code_executor=BuiltInCodeExecutor())
